first_name = "Max"
last_name = "Zimmer"

print(first_name, last_name)

name_list = [first_name, last_name]

print(name_list)

name_dict = {
    "first": first_name,
    "last": last_name
}

print(name_dict)
